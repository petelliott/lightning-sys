language: rust

os: linux

dist: bionic

arch:
  - amd64

rust:
  - nightly
  - beta
  - stable

jobs:
  include:
    - rust: stable
      arch: arm64
    - rust: stable
      arch: s390x
    - rust: stable
      arch: ppc64le
    - rust: stable
      os: osx

  allow_failures:
    - rust: nightly

script:
  - cargo test --verbose

notifications:
  email: false
